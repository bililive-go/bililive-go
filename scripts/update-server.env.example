# bgo 本地升级测试服务器配置示例
# 复制为 update-server.env 后填写实际值；该实际配置文件已被 .gitignore 忽略。

# HTTP 服务监听地址。需要供局域网/NAS 访问时通常设为 0.0.0.0。
BGO_UPDATE_BIND_HOST=127.0.0.1

# HTTP 服务监听端口。
BGO_UPDATE_PORT=8099

# 写入版本 API 下载 URL 的主机名或 IP；请替换为 NAS 能访问的开发机地址。
BGO_UPDATE_PUBLIC_HOST=192.0.2.10

# 已构建的升级包路径，可使用相对仓库根目录的路径。
BGO_UPDATE_PACKAGE=bin/bililive-linux-amd64.tar.gz

# 返回给 bgo 的目标版本号。
BGO_UPDATE_VERSION=0.0.0-local.1
